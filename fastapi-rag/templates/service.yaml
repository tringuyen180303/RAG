apiVersion: v1
kind: Service
metadata:
  name: {{ include "fastapi-rag.fullname" . }}
  labels:
    {{- include "fastapi-rag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fastapi-rag.selectorLabels" . | nindent 4 }}
